<h1>Reports</h1>
<table class="table table-striped mt-5 table-dark">
  <tr>
    <th>Employee name</th>
    <th>Today attendance</th>
  </tr>
  <% @changed_employees.each do |employee| %>
    <tr>
      <td><%= employee[:name] %></td>
      <td><%= employee[:today_assist] ? "✔" : "✖" %></td>
    </tr>
  <% end %>
</table>
